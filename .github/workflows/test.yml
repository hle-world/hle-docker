name: Test

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - 'frontend/**'
      - 'Dockerfile'
      - '.github/workflows/test.yml'
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lint-backend:
    name: Lint backend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@692973e3d937d1afb5f8355e64d32566a30ded47

      - uses: actions/setup-python@f677139edd7d1d27f0684c23c28b75935482262e
        with:
          python-version: '3.12'

      - name: Install ruff
        run: pip install ruff

      - name: Lint
        run: ruff check backend/

      - name: Check formatting
        run: ruff format --check backend/

  check-frontend:
    name: Check frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@692973e3d937d1afb5f8355e64d32566a30ded47

      - uses: actions/setup-node@1f8c6b94b26d0feae1666245b2fb7324019d73bf
        with:
          node-version: '22'

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      - name: TypeScript check
        working-directory: frontend
        run: npx tsc --noEmit

      - name: Build
        working-directory: frontend
        run: npm run build

  security:
    name: Security scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@692973e3d937d1afb5f8355e64d32566a30ded47

      - uses: actions/setup-python@f677139edd7d1d27f0684c23c28b75935482262e
        with:
          python-version: '3.12'

      - name: Install tools
        run: pip install bandit pip-audit hle-client fastapi uvicorn

      - name: Bandit SAST
        run: bandit -r backend/ -ll

      - name: Dependency audit
        run: pip-audit

      - name: Secret scan
        uses: trufflesecurity/trufflehog@c3e599b7163e8198a55467f3133db0e7b2a492cb
        with:
          extra_args: --only-verified
