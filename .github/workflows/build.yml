name: Build & Publish Docker Image

on:
  release:
    types: [published]
  workflow_dispatch:

env:
  IMAGE: ghcr.io/hle-world/hle-docker

jobs:
  build:
    name: Build multi-arch image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to ghcr.io
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version
        id: version
        env:
          EVENT_NAME: ${{ github.event_name }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          SHA: ${{ github.sha }}
        run: |
          if [ "${EVENT_NAME}" = "release" ]; then
            VERSION="${RELEASE_TAG#v}"
            echo "tags=${IMAGE}:${VERSION},${IMAGE}:latest" >> "$GITHUB_OUTPUT"
          else
            echo "tags=${IMAGE}:dev-${SHA}" >> "$GITHUB_OUTPUT"
          fi

      - name: Extract headless tags
        id: headless-version
        env:
          EVENT_NAME: ${{ github.event_name }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          SHA: ${{ github.sha }}
        run: |
          if [ "${EVENT_NAME}" = "release" ]; then
            VERSION="${RELEASE_TAG#v}"
            echo "tags=${IMAGE}:${VERSION}-headless,${IMAGE}:headless" >> "$GITHUB_OUTPUT"
          else
            echo "tags=${IMAGE}:dev-${SHA}-headless" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and push (full)
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.version.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push (headless)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.headless
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.headless-version.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
