name: Sync Frontend from hle-webapp

# Triggered by hle-webapp after it builds a new release.
# Downloads the pre-built docker frontend artifact and opens a PR
# updating frontend/dist/ -- the Dockerfile COPYs this directory directly.

on:
  repository_dispatch:
    types: [webapp-release]
  workflow_dispatch:
    inputs:
      version:
        description: "hle-webapp version to sync (e.g. 2604.1.0)"
        required: true
      hle_client_version:
        description: "hle-client version to pin in Dockerfiles (optional)"
        required: false

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ github.event.client_payload.version || github.event.inputs.version }}
      CLIENT_VERSION: ${{ github.event.client_payload.hle_client_version || github.event.inputs.hle_client_version }}
    steps:
      - uses: actions/checkout@v6

      - name: Download docker frontend artifact
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
        run: |
          mkdir -p frontend/dist
          rm -rf frontend/dist/*
          curl -sL \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            "https://github.com/hle-world/hle-webapp/releases/download/v${VERSION}/hle-frontend-docker-${VERSION}.tar.gz" \
            | tar -xz -C frontend/dist

      - name: Bump hle-client pin in Dockerfiles
        if: env.CLIENT_VERSION != ''
        run: |
          CURRENT=$(grep -oE 'hle-client==[0-9][0-9.]*' Dockerfile | head -1 | sed 's/hle-client==//')
          if [ "$CURRENT" != "$CLIENT_VERSION" ]; then
            sed -i "s|hle-client==[0-9][0-9.]*|hle-client==${CLIENT_VERSION}|g" Dockerfile Dockerfile.headless
            echo "Bumped hle-client pin: $CURRENT -> $CLIENT_VERSION"
          else
            echo "hle-client pin already at $CLIENT_VERSION — no change."
          fi

      - name: Open sync PR
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
        run: |
          if git diff --quiet && [ -z "$(git ls-files --others --exclude-standard frontend/dist/)" ]; then
            echo "No changes -- skipping PR."
            exit 0
          fi

          BRANCH="chore/sync-webapp-${VERSION}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "${BRANCH}"
          git add frontend/dist/ Dockerfile Dockerfile.headless
          if [ -n "$CLIENT_VERSION" ]; then
            COMMIT_MSG="chore: sync from hle-webapp v${VERSION} (hle-client v${CLIENT_VERSION})"
            BODY="Pre-built docker frontend from hle-webapp v${VERSION}. hle-client pin bumped to v${CLIENT_VERSION} in Dockerfile and Dockerfile.headless."
          else
            COMMIT_MSG="chore: sync frontend from hle-webapp v${VERSION}"
            BODY="Pre-built docker frontend from hle-webapp v${VERSION}."
          fi
          git commit -m "${COMMIT_MSG}"
          git push -u origin "${BRANCH}"
          gh pr create \
            --title "${COMMIT_MSG}" \
            --body "${BODY}" \
            --base main
