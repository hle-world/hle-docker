name: Auto-release on PR merge

# Creates a GitHub release when any PR is merged to main.
# This triggers the Docker build workflow (build.yml).
#
# Version logic:
#   Version is always <hle-client>.<patch>, derived from the hle-client pin
#   in Dockerfile (the source of truth):
#     - If the pin's MAJOR.MINOR differs from the latest release tag's
#       MAJOR.MINOR, reset to <client>.0 (new patch series).
#     - Otherwise bump the patch digit.
#
# Skip release: add [skip release] to the PR title

on:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  release:
    name: Create release
    if: >-
      github.event.pull_request.merged == true &&
      !contains(github.event.pull_request.title, '[skip release]')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Determine version
        id: ver
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
        run: |
          CLIENT_VER=$(grep -oE 'hle-client==[0-9][0-9.]*' Dockerfile | head -1 | sed 's/hle-client==//')
          if [ -z "$CLIENT_VER" ]; then
            echo "::error::could not parse hle-client pin from Dockerfile"
            exit 1
          fi

          LATEST=$(gh release view --json tagName -q .tagName 2>/dev/null || echo "v0.0.0")
          LATEST="${LATEST#v}"
          LATEST_MM=$(echo "$LATEST" | cut -d. -f1-2)

          if [ "$CLIENT_VER" != "$LATEST_MM" ]; then
            # New client MAJOR.MINOR — start a fresh patch series.
            VERSION="${CLIENT_VER}.0"
            echo "source=client-bump" >> "$GITHUB_OUTPUT"
          else
            PATCH=$(echo "$LATEST" | cut -d. -f3)
            VERSION="${CLIENT_VER}.$((PATCH + 1))"
            echo "source=patch-bump" >> "$GITHUB_OUTPUT"
          fi
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Create release
        env:
          GH_TOKEN: ${{ secrets.HLE_PAT }}
          VERSION: ${{ steps.ver.outputs.version }}
          SOURCE: ${{ steps.ver.outputs.source }}
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          if [[ "$SOURCE" == "client-bump" ]]; then
            CLIENT_VER="${VERSION%.*}"
            NOTES="Bumped to hle-client v${CLIENT_VER}. See [hle-client release notes](https://github.com/hle-world/hle-client/releases/tag/v${CLIENT_VER}) for details. (${PR_TITLE} #${PR_NUMBER})"
          else
            NOTES="${PR_TITLE} (#${PR_NUMBER})"
          fi
          gh release create "v${VERSION}" \
            --title "v${VERSION}" \
            --notes "$NOTES"

      # The "Notify hle-webapp of changes" dispatch was removed alongside
      # the backwards sync workflow in hle-webapp (PR #42 there).
      # hle-webapp is the source of truth for the frontend; hle-docker
      # consumes from it via sync-webapp.yml, never the other way around.
