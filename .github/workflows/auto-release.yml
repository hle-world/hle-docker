name: Auto-release on sync PR merge

# Triggered when a sync/hle-client-v* PR is merged into main.
# Creates a GitHub release, which triggers the Docker build workflow.
# Unlike ha-addon, no pre-release/promote pattern is needed — Docker users
# pull by tag, and the tag only exists after the build completes.

on:
  pull_request:
    types: [closed]
    branches: [main]

jobs:
  release:
    name: Create release
    if: github.event.pull_request.merged == true && startsWith(github.event.pull_request.head.ref, 'sync/hle-client-v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Read target version from Dockerfile
        id: ver
        run: |
          VERSION=$(grep 'hle-client==' Dockerfile | sed 's/.*hle-client==\([0-9][0-9.]*\).*/\1/')
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Create release
        env:
          # Must use a PAT here — GITHUB_TOKEN-created releases do not cascade
          # to trigger other workflows (GitHub anti-loop protection).
          GH_TOKEN: ${{ secrets.HLE_PAT }}
          VERSION: ${{ steps.ver.outputs.version }}
        run: |
          gh release create "v${VERSION}" \
            --title "v${VERSION}" \
            --notes "Synced to hle-client v${VERSION}. See [hle-client release notes](https://github.com/hle-world/hle-client/releases/tag/v${VERSION}) for details."
